//
// Created by Saurav Lamichhane on 10/24/23.
//

#include <stdbool.h>
#include <printf.h>

int main() {

    //Necessary variables
    int s;
    int e;
    int b;
    int m;
    char policy;
    int hitTime;
    int missPenalty;

    //Initialize the variables
        scanf("Enter the number of sets (S): %d \n", &s);
        scanf("Enter the number of lines per set (E): %d", &e);
        scanf("Enter the block size B: %d", &b);
        scanf("Enter the number of physical memory bits (m): %d", &m);
        scanf("Choose a policy: [L : %d", &s);
        scanf("Enter the hit time : %d", &hitTime);
        scanf("Enter the miss penality : %d", &missPenalty);

    }

}